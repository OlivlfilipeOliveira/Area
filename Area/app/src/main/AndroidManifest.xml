<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pc.area">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".trapezi" />
        <activity android:name=".circulo" />
        <activity android:name=".triangulo" />
        <activity android:name=".retangulo" />
        <activity android:name=".quadrado" />
        <activity android:name=".losango" />
        <activity android:name=".exemploQuadrado" />
        <activity android:name=".exemploRetangulo" />
        <activity android:name=".exemploCirculo" />
        <activity android:name=".exemploTrapezio" />
        <activity android:name=".exemploLosango" />
        <activity android:name=".exemploTriangulo"></activity>
    </application>

</manifest>
